# Database Configuration
# Choose one of the following database options:

# Option 1: Supabase Database
# Get these from your Supabase project dashboard
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Option 2: MongoDB Database
# Use either MONGODB_URI or DATABASE_URL
MONGODB_URI=mongodb://localhost:27017/znforge
# OR
DATABASE_URL=mongodb://localhost:27017/znforge

# Option 3: Use in-memory storage (default)
# No environment variables needed - will automatically use memory storage
# if no database credentials are provided

# Development Settings
NODE_ENV=development

# Production Settings (when deploying)
# NODE_ENV=production